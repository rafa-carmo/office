type ServiceOrder {
    id: Int
    brand: String
    model: String
    defect: String
    valueService: Float
    valueMaterial: Float
    isAuthorized: Boolean
    isReady: Boolean
    wasClosed: Boolean
    date: DateTime
    services: [Service]
    type: Type
    client: Client
    pictures: [Picture]
    budget: [Budget]

}

input ServiceId {
    id: Int!
}