type Client {
    id: Int
    name: String
    phone: [Phone]
    email: String
    address: String
    date: DateTime
    serviceOrder: [ServiceOrder]
}

type Phone {
    phone: String
    isWhats: Boolean
    client: Client
}

input searchClient {
    name: String
}